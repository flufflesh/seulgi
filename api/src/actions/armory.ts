import { Router } from "express"
// import BlizzardArmory from "../services/BlizzardArmory.service"

const router = Router()
// const blizzardArmory = new BlizzardArmory(
// )

// router.post("/character", async (req, res) => {
//   const { characterName: name, realm, region } = req.body.character

//   const chara = await blizzardArmory.getCharacter(region, name, realm)
//   return res.json(chara)
// })

export default router
